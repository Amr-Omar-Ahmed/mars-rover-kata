{
  "name": "mars-rover-kata",
  "version": "1.0.0",
  "description": "API translate commands sent from earth to instructions that are understood bt the rover",
  "main": "main.js",
  "scripts": {
    "start": "tsc -p . && node dist/main.js",
    "dev": "NODE_ENV=dev nodemon src/main.ts",
    "test": "NODE_ENV=test mocha -r ts-node/register  \"src/tests/**/*.spec.ts\"",
    "test-watch": "NODE_ENV=test mocha -r ts-node/register  \"src/tests/**/*.spec.ts\"  --watch --watch-files \"src/tests/**/*.spec.ts\""
  },
  "keywords": [
    "tdd",
    "mars-rover-kata"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/chai": "^4.2.14",
    "@types/express": "^4.17.13",
    "@types/mocha": "^8.0.4",
    "@types/node": "^14.14.7",
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "nodemon": "^2.0.9",
    "ts-node": "^10.0.0",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.3.5"
  }
}
